// globals.css is plain CSS — no PostCSS plugins needed. This empty config
// exists so Next.js does not walk up and inherit a parent directory's config.
const config = {
  plugins: {},
};

export default config;
